@using BlazorApp1.Model
@typeparam TItem
<h3>TemplatedChildComponent</h3>
<div>
    @foreach(TItem p in Items){
        @ChildContent(p)}
</div>

@code {
    [Parameter]
    public List<TItem> Items{ get; set; }
    [Parameter]
    public RenderFragment<TItem> ChildContent { get; set; }
}
